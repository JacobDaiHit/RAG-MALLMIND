"""Small shared utilities with no business-routing authority.

Only runtime-safe error formatting remains here. Legacy generic RAG helpers,
parent-chunk merging, and catalog-scope normalization were removed because the
V3 chain owns those responsibilities explicitly.
"""
