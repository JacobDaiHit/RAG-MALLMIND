[pytest]
addopts = -m "not integration"
markers =
    unit: fast tests that do not require local services or heavyweight models
    integration: tests that require external services, local model runtimes, or large optional dependencies
    milvus: tests that require a running Milvus service and embedding model runtime
    pdf: tests that require PDF parsing dependencies and fixture files
