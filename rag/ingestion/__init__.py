"""Catalog-to-vector-index ingestion package.

``product_chunks`` converts real catalog products into evidence chunks and
``embedding`` provides the configured embedding implementation. CLI scripts,
not package import, trigger indexing or external model calls.
"""
